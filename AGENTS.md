# Repository workflow

- Commit and push coherent implementation milestones regularly; do not leave a large completed body of work unpushed while continuing unrelated work.
- After every project commit pushed from the main development branch, update the `minimal` branch to consume that exact commit, verify its strict-minimum setup, then commit and push `minimal` too.
