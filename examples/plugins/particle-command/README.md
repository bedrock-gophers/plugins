# Particle command

Typed world particle examples using the source player's actual managed world and position.

Commands: `/particle flame`, `/particle coloured-flame`, `/particle dust`, `/particle block-break`, `/particle punch-block`, `/particle bone-meal`, `/particle note`, `/particle dragon-egg`, `/particle explosion`, `/particle splash`, `/particle effect`.
