# Entity command

Typed entity handles, built-in entity spawning, projectiles, state lookup, and world entity/player lists.

Commands: `/entity text`, `/entity lightning`, `/entity tnt`, `/entity snowball`, `/entity sword`, `/entity sand`, `/entity list`.
