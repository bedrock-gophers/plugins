# Minimal bedrock-gophers server

Consumer-only example. This branch contains no bedrock-gophers framework/runtime source.

```shell
make run
```

One Rust plugin lives under `plugins/movement-guard`. Build fetches pinned bedrock-gophers revision, compiles runtime and plugin, then starts server.

