# Entity command

Typed entity handles, built-in entity spawning, automatically registered persistent base entities, projectiles, state lookup, and world entity/player lists.

Commands: `/entity text`, `/entity marker`, `/entity lightning`, `/entity tnt`, `/entity snowball`, `/entity sword`, `/entity sand`, `/entity list`.
