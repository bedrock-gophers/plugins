# Chat filter

Mutable chat example. It replaces `foo` with `bar` and cancels a message equal to `blocked`.

```shell
cargo build --release -p chat-filter-plugin
```

Output: `target/release/libchat_filter_plugin.so` on Linux.

