# Sound command

Typed private-player and world sound examples using the source player's actual managed world.

Commands: `/sound player`, `/sound explosion`, `/sound door`, `/sound note`, `/sound equip`, `/sound bucket`, `/sound disc`, `/sound horn`, `/sound attack`, `/sound crossbow`.
